<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:background="@drawable/bg_song_row"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:clickable="true"
    android:paddingHorizontal="8dp">

    <!-- Source badge (cloud / phone) -->
    <TextView
        android:id="@+id/tv_source"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:gravity="center"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Title + artist + album -->
    <LinearLayout
        android:id="@+id/text_group"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginStart="4dp"
        app:layout_constraintStart_toEndOf="@id/tv_source"
        app:layout_constraintEnd_toStartOf="@id/meta_group"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/on_surface"/>

        <TextView
            android:id="@+id/tv_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/on_bg_secondary"/>

        <TextView
            android:id="@+id/tv_album"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/on_bg_tertiary"/>
    </LinearLayout>

    <!-- Right: format + duration + action buttons -->
    <LinearLayout
        android:id="@+id/meta_group"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tv_format"
            android:layout_width="36dp"
            android:layout_height="22dp"
            android:gravity="center"
            android:textSize="10sp"
            android:textStyle="bold"
            android:textColor="@color/primary"
            android:background="@drawable/bg_format_badge"
            android:layout_marginEnd="8dp"/>

        <TextView
            android:id="@+id/tv_duration"
            android:layout_width="44dp"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/on_bg_secondary"
            android:gravity="end"
            android:layout_marginEnd="8dp"/>

        <!-- Favorite button -->
        <ImageButton
            android:id="@+id/btn_favorite"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_favorite_border"
            android:background="@drawable/bg_icon_btn"
            android:padding="10dp"
            android:focusable="true"
            android:contentDescription="收藏"/>

        <!-- Add to queue -->
        <ImageButton
            android:id="@+id/btn_queue"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_queue_add"
            android:background="@drawable/bg_icon_btn"
            android:padding="10dp"
            android:focusable="true"
            android:contentDescription="加入队列"/>

        <!-- Play -->
        <ImageButton
            android:id="@+id/btn_play"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_play"
            android:background="@drawable/bg_play_btn_small"
            android:padding="12dp"
            android:focusable="true"
            android:contentDescription="播放"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
